export type FormData = {
    Name: string
    Email: string
    CPF: string
    Identidade: string
    senha: string
    cidade: string
    estado: string
    sexo: string
};

export type Localidade = {
    id: number;
    nome: string;
    sigla?: string;
};

